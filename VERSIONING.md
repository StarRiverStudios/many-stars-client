# 星空神域 - 版本号设计方案与更新日志模板
## 版本号说明
本版本号专用于 **「繁星计划」** 下属项目，参考 **语义化版本（SemVer 2.0.0）** 的语法风格，并扩展项目版本与构建信息。
版本号结构：`MAP.MCV.LDV.ADV(-PRERELEASE)+BUILDCODE`

### 版本号各字段含义
- **`MAP`**：地图版本号
  - 更新条件：每三年由玩家投票决定是否重置地图，表决通过后递增。
  - 初始值：`1`
- **`MCV`**：游戏版本号
  - 更新条件：Minecraft 游戏版本更新并经项目组评估与测试通过后递增。
  - 初始值：`0`
- **`LDV`**：加载器版本号
  - 更新条件：模组加载器更新并测试通过后递增。
  - 初始值：`0`
- **`ADV`**：组件版本号
  - 更新条件：核心组件、内容组件、语言组件更新后递增。
  - 初始值：`0`

### 预发布标识
**`-PRERELEASE`** 用于标识正式版发布前的测试阶段；无此后缀即为正式版。每次地图迭代后依次进行 **Beta → RC → 正式版**：
- **Beta**：内部测试版，仅限整合包制作团队和报名测试的玩家使用。允许调整 Mod 及其他资源，持续稳定一周后进入 RC 阶段。
  - 示例：`1.0.0.0-beta.1`
- **RC（Release Candidate）**：公开候选版，对玩家开放测试。进入 RC 后冻结 Mod 列表及主要内容，不再进行功能性内容扩展。每个 RC 测试一周；发现问题则修复并发布下一个 RC。
  - 示例：`1.0.0.0-rc.1`、`1.0.0.0-rc.2`

### 构建编号
**`+BUILDCODE`** 用于内部构建追踪，格式为：`YYMN[P]`
- **`YY`**：两位年份，如 `26` 代表 2026 年。
- **`M`**：地图周期标识，从 `A` 开始，每次进入新地图周目递增，`Z` 后循环至 `A`。
- **`N`**：全局构建序号，从 `1` 开始递增，不设上限，不因地图重置归零。
- **`P`**：可选的构建类型标识。
  - `b`：Beta 测试构建
  - `r`：RC 候选构建
  - `s`：Special 特殊构建
  - 无标识：正式发行构建

### 版本号递增规则
高位版本号递增时，其右侧低位版本号全部重置为 `0`：
- `MAP`：`1.2.3.4` → `2.0.0.0`
- `MCV`：`1.2.3.4` → `1.3.0.0`
- `LDV`：`1.2.3.4` → `1.2.4.0`
- `ADV`：`1.2.3.4` → `1.2.3.5`
`+BUILDCODE` 不参与版本号归零，构建序号 `N` 持续递增。
RC 测试通过后，去除 `-PRERELEASE` 后缀和构建号中的 `P` 标识，版本主体保持不变。

## 示例
### `1.0.0.0-beta.1+26A1b`
- `MAP`：第 1 个地图周期
- `MCV`：游戏版本 0
- `LDV`：加载器版本 0
- `ADV`：组件版本 0
- `Beta.1`：第 1 个 Beta 测试版本
- `26A1b`：2026 年、地图周期 A、第 1 次构建、Beta 构建

### `1.0.0.0-rc.2+26A15r`
- `MAP`：第 1 个地图周期
- `MCV`：游戏版本 0
- `LDV`：加载器版本 0
- `ADV`：组件版本 0
- `RC.2`：第 2 个 RC 候选版本
- `26A15r`：2026 年、地图周期 A、第 15 次构建、RC 构建

### `3.2.5.1+32C128`
- `MAP`：第 3 个地图周期
- `MCV`：游戏版本 2
- `LDV`：加载器版本 5
- `ADV`：组件版本 1
- 无预发布标识：正式版
- `32C128`：2032 年、地图周期 C、第 128 次正式构建

## 更新模板
```markdown
# 星空神域 - 客户端更新日志 (`PROJECT/MAP.MCV.LDV.ADV(-PRERELEASE)+BUILDCODE`)
**发布日期：** YYYY-MM-DD

子组件版本：
- 内容组件：`MAJOR.MINOR.PATCH(-PRERELEASE)`
- 语言组件：`MAJOR.MINOR.PATCH(-PRERELEASE)`

> ⚠️ 此为 **Beta** 测试版（或 **Release Candidate** 发布候选版），可能存在兼容性问题，欢迎反馈！（仅 Beta、RC 版本添加）
> 此版本为强制更新，如不更新可能会出现兼容性问题甚至无法正常进入服务器（仅强制更新版本添加）

## 更新介绍
（占位）

## 分发标准
- 采用 **Modrinth 整合包格式**（`.mrpack`）打包，通过 **GitHub Release** 分发
- 所有资源由 **Packwiz** 统一管理
- 此为 **「繁星计划」** 下属客户端统一规范

## 变更说明
### 技术栈变更
- **更新**
  - （占位）

### 模组变更
- **新增**
  - （占位）
  - **前置模组**
    - （占位）
- **删除**
  - （占位）
  - **前置模组**
    - （占位）
- **更新**
  - （占位）
  - **前置模组**
    - （占位）

### 资源变更
- **新增**
  - （占位）
- **删除**
  - （占位）
- **更新**
  - （占位）

## 使用指南
详细系统要求、启动器配置与安装步骤，请参阅项目 README：
- 简体中文版：[README.md](https://github.com/StarRiverStudios/many-stars-client/blob/main/README.md)
- 繁體中文版：[README_TW.md](https://github.com/StarRiverStudios/many-stars-client/blob/main/README_TW.md)
- English version：[README_EN.md](https://github.com/StarRiverStudios/many-stars-client/blob/main/README_EN.md)

## 加入社区
- QQ 群：[点击加入](https://qm.qq.com/q/RgessVyPC0)
- Discord：[Join Server](https://discord.gg/ekpaH4FXDF)
```